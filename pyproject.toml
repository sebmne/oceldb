[project]
name = "oceldb-dev"
version = "0.0.0"
requires-python = ">=3.11"
dependencies = [
  "oceldb",
  "oceldb-ui",
]

[tool.uv.sources]
oceldb = { workspace = true }
oceldb-ui = { workspace = true }

[tool.uv.workspace]
members = [
  "packages/oceldb",
  "packages/oceldb-ui",
]
